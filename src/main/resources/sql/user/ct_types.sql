CREATE TABLE TYPES
(
  ID BIGINT AUTO_INCREMENT PRIMARY KEY NOT NULL,
  NAME VARCHAR(256) NOT NULL
);
ALTER TABLE TYPES ADD CONSTRAINT TYPES_unique_ID UNIQUE (ID);
ALTER TABLE TYPES ADD CONSTRAINT TYPES_unique_NAME UNIQUE (NAME);