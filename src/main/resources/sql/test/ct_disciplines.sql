CREATE TABLE DISCIPLINES
(
  ID BIGINT AUTO_INCREMENT PRIMARY KEY NOT NULL,
  NAME VARCHAR(256) NOT NULL,
  DELETED BOOLEAN NOT NULL
);
ALTER TABLE DISCIPLINES ADD CONSTRAINT DISCIPLINES_unique_ID UNIQUE (ID);
ALTER TABLE DISCIPLINES ADD CONSTRAINT DISCIPLINES_unique_NAME UNIQUE (NAME);